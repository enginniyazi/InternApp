{
  "installCommand": "npm install --legacy-peer-deps && npm --prefix apps/client install --legacy-peer-deps",
  "buildCommand": "npm --prefix apps/client run build",
  "outputDirectory": "apps/client/dist",
  "framework": "vite"
}
